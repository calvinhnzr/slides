export const headline = "What's the Problem?"
export const layout = "subslide"
export const slideClasses = "center"

### {headline}

{/* ### The problem is that we have a lot of data and we need to make sense of it. */}
